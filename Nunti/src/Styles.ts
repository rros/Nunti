import { StyleSheet } from "react-native";
import { DarkTheme, DefaultTheme } from 'react-native-paper';

// customise default dark and white themes later
/*export const Dark = {
    ...DarkTheme,
    colors: {
        ...DarkTheme.colors,
        accentName: "aqua",
        primary: "#3498db",
        accent: '#3498db',
    }
};

export const Light = {
    ...DefaultTheme,
    colors: {
        ...DefaultTheme.colors,
        accentName: "aqua",
        primary: "#3498db",
        accent: '#3498db',
    }
};*/

// custom css here
export default Styles = StyleSheet.create ({

});

